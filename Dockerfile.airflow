# Dockerfile.airflow
FROM apache/airflow:2.10.2

COPY requirements.txt /requirements.txt

# Airflow 2.10.2 default image uses Python 3.12 → use matching constraints
RUN pip install --no-cache-dir -r /requirements.txt 

